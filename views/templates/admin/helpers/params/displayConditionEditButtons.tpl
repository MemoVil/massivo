<td  recipe="{$recipe->id}" step="{$step->id}" row="{$row}">
	<button type="button" class="btn btn-danger canceleditcondition" recipe="{$recipe->id}" step="{$step->id}" row="{$row}">
		<i class="icon-trash"></i>
	</button>
	<button type="button" class="btn btn-success saveeditcondition" recipe="{$recipe->id}" step="{$step->id}" row="{$row}">
	    <i class="icon-check"></i>
	</button>     
</td>
