<td  recipe="{$recipe->id}" step="{$step->id}" row="{$row}" cid="{$condition->getId()}">
	<button type="button" class="btn btn-danger canceleditcondition" recipe="{$recipe->id}" step="{$step->id}" row="{$row}" cid="{$condition->getId()}">
		<i class="icon-trash"></i>
	</button>
	<button type="button" class="btn btn-success saveeditcondition" recipe="{$recipe->id}" step="{$step->id}" row="{$row}" cid="{$condition->getId()}">
	    <i class="icon-check"></i>
	</button>     
</td>
